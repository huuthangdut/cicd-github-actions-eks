apiVersion: v1
kind: Service
metadata:
  name: app-demo-service
spec:
  selector:
    app: app-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  # type: ClusterIP
  type: LoadBalancer